class City:
    def __init__(self, rows, cols, walk_dist, n_building_projs):
        self.rows = int(rows)
        self.cols = int(cols)
        self.walk_dist = int(walk_dist)
        self.n_building_projs = int(n_building_projs)